@page
@model BRIDGEWebApp.Pages.Survey.DetailsModel

@{
    ViewData["Title"] = "Survey Details";
}

<h1>Survey Details</h1>
<div>
    <a type="button" class="btn btn-sm btn-success" asp-page="./Edit" asp-route-id="@Model.Survey?.Id">Edit</a>
    <a type="button" class="btn btn-sm btn-primary" asp-page="./Index">Back to List</a>
</div>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Survey.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Survey.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Survey.CreatedOn)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Survey.CreatedOn)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Survey.IsActive)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Survey.IsActive)
        </dd>
    </dl>
</div>

