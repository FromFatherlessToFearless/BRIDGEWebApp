@page "/cohort/{id?}"
@model BRIDGEWebApp.Pages.Cohort.DetailsModel

@{
    ViewData["Title"] = "Cohort - " + Model.Cohort.Id;
}

<h1>Cohort @Model.Cohort.Id</h1>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Cohort?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
<hr />
<h2>@Model.Cohort?.Name</h2>
<p>@Model.Cohort?.Description</p>



<div>
   <dl>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Cohort.StartDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Cohort.StartDate)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Cohort.IsRegistrationOpen)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Cohort.IsRegistrationOpen)
        </dd>
        
    </dl>
</div>

